https://chatgpt.com/share/6a028c64-5988-8321-a1cd-736257a092dc

## windowS mitmproxy 证书 位置

C:\Users\a1829\.mitmproxy\mitmproxy-ca-cert.cer

## bash命令下计算 Android 证书 hash

```bash
"C:\Program Files\Git\usr\bin\openssl.exe" x509 -inform PEM -subject_hash_old -in mitmproxy-ca-cert.cer
```
会得到 c8750f0d 类似 记住的证书 hash

cp mitmproxy-ca-cert.cer c8750f0d.0


```bash
adb push c8750f0d.0 /system/etc/security/cacerts/ （这一步走不通一般 ，使用下面方法去绕过）

```

``` bash
# 按顺序执行（很重要）

# 前提：你已经 root 了，在 su shell 下执行。

# 1. 备份原证书目录到临时目录
cp -R /system/etc/security/cacerts /data/local/tmp/cacerts_backup
# 2. mount tmpfs 覆盖原目录（最重要的一步） 利用 Linux mount 机制在运行时替换系统看到的目录 弊端就是重启每次都得来一遍 
mount -t tmpfs tmpfs /system/etc/security/cacerts

# 如果成功不会有输出。

# 3. 把原证书复制回来
cp -R /data/local/tmp/cacerts_backup/* /system/etc/security/cacerts/
# 4. 推你的 mitm 证书进去

# 先确认证书在 sdcard：

ls /sdcard/c8750f0d.0

# 然后：

cp /sdcard/c8750f0d.0 /system/etc/security/cacerts/
# 5. 修权限
chmod 644 /system/etc/security/cacerts/c8750f0d.0
chown root:root /system/etc/security/cacerts/c8750f0d.0
# 6. 检查是否存在
ls /system/etc/security/cacerts | grep c8750f0d

# 应该看到：

c8750f0d.0
# 7. 重启 app（不用重启系统）

# 然后重新：

# pkill -f MsgSender

# 或者手动关 app 再开。

# 再测广告。

# 注意

# 这个 overlay 是临时的。

# 模拟器重启后失效。
# 
# 但先验证是否有效。

# 如果广告能过，再考虑做自动脚本或 Magisk 持久化。

# 所以现在你终于有一条不靠 Magisk 的捷径了。

```
## 测试
adb shell am start -a android.intent.action.VIEW -d https://baidu.com 

## 重启后的完整脚本

可直接执行[start]("./start.bat")





















<!-- 后面的就是本次尝试的其他没跑通的方案了 -->
<!-- ## 装magisk  -->
<!-- [Magisk](https://github.com/topjohnwu/Magisk?utm_source=chatgpt.com)
[AlwaysTrustUserCerts](https://github.com/NVISOsecurity/AlwaysTrustUserCerts?utm_source=chatgpt.com) -->
<!-- 安装后验证 adb shell ； su ； magisk -v 发现并不能使用 还是被system修改文件限制了 -->


## 1.1Frida SSL Unpinning 
https://github.com/frida/frida/releases?utm_source=chatgpt.com
下载 ：frida-server-17.9.8-android-x86_64 (注意这个包 我下了三回 兼容问题 必须特定版本)
解压后 push 到设备 

adb push C:\Users\a1829\Documents\download\frida-server-17.9.8-android-x86_64 /data/local/tmp/

adb shell chmod 755 /data/local/tmp/frida-server-17.9.8-android-x86_64/frida-server-17.9.8-android-x86_64
###  1.2启动
``` bash
adb shell 
su
/data/local/tmp/frida-server-17.9.8-android-x86_64/frida-server-17.9.8-android-x86_64 &
```
### 1.3 PC 安装 frida tools 
pip install frida-tools

测试：frida-ps -U   
能看到进程列表system_server就成功。

ps:查包
adb shell pm list packages | findstr fycc
kill 老 frida  : pkill frida-server

### 1.4 启动 universal ssl bypass
``` bash
pip install objection
```
## 1.5 然后pc运行
``` bash

# 这一步app需要先启动

# 第一种普通启动
objection -g com.MsgSender.io.fycc start
# 最想做的事情
android sslpinning disable 

# 第二种 脚本启动 有可能干得太猛 把app奔溃 (好处:过更多的https 这里主要过广告)
frida -U -f com.MsgSender.io.fycc --codeshare avltree9798/universal-android-ssl-pinning-bypass
# 或者
frida -U -f com.MsgSender.io.fycc --codeshare akabe1/frida-multiple-unpinning

# 然后保持这个 objection 不关  
# 如果关机 就重新启动1.2 + 1.5 部分
```

或者直接注入 script。

这样：

app SSL pinning 被关
用户证书可用
mitmproxy 能抓 HTTPS 

## 重启 

mitmweb --listen-host 0.0.0.0 --listen-port 8080 

## 测试
adb shell am start -a android.intent.action.VIEW -d https://baidu.com （走frida方案这一步是需要针对浏览器进行配置的）

连接问题：net::ERR TIMED OUT

证书是否真的安装成功 ： 

adb shell
su
ls /system/etc/security/cacerts/ | grep c8750f0d












# 广告证书

##  https://chatgpt.com/c/6a02909c-7730-8323-ba7c-db568a4b07d0


当app有广告的时候 https 验证失效 需要判断广告以什么方式启动 比如ads子进程 webview等等

## 以下进行是为了webview等广告过https 方案：额外 hook WebView SSL

在 objection 里执行：

android hooking watch class android.webkit.WebViewClient

1. 或者直接用通杀脚本（比 objection 更全）：[Universal Android SSL Pinning Bypass script](https://codeshare.frida.re/@avltree9798/universal-android-ssl-pinning-bypass/?utm_source=chatgpt.com)

```bash
# 脚本会自动在线拉取  如果你用 --codeshare，就是 Frida 直接启动 + 注入脚本，objection 不参与
# 把 1.5 的 objection -g com.MsgSender.io.fycc start 改为下面这段
frida -U -f com.MsgSender.io.fycc --codeshare avltree9798/universal-android-ssl-pinning-bypass

# 这个覆盖更多： frida -U -f com.MsgSender.io.fycc --codeshare akabe1/frida-multiple-unpinning

# WebView
# Trustkit
# Cronet
# Apache Harmony
# Xutils
# Flutter
# Conscrypt
```


